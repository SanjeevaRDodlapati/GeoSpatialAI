apiVersion: v1
kind: Service
metadata:
  name: workflow-engine
  namespace: conservation-prod
  labels:
    app: workflow-engine
    component: workflow_engine
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
  selector:
    app: workflow-engine