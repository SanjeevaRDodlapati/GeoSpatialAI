apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: conservation-prod
  labels:
    app: redis
    component: message_queue
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
  selector:
    app: redis